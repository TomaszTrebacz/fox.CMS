scalar Date

type Post @key(fields: "id") {
  id: Int!
  title: String!
  text: String!
  created: Date!
  user: User
}

extend type User @key(fields: "id") {
  id: String! @external
  posts: [Post]
}

extend type Query {
  posts: [Post]
}
